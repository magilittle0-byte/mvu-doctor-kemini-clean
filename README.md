# MVU 人物与世界医生（Kemini Clean）

> 测试候选版：尚未执行真实 TauriTavern / SillyTavern 十二回合验收，不能视为正式发布或“已经可用”的证明。本仓库供用户自行安装实测。

当前版本：`0.1.1`。运行时核心已经打包进 `index.js`，不依赖 `document.currentScript` 或额外定位 `core.mjs`，兼容以非脚本标签方式执行扩展入口的酒馆加载器。

这是一个从零写的轻量医生扩展。它不继承旧医生的任务编排和臃肿架构，与本地交付的 **Kemini Dramatron 陨落的天才 v1.2 MVU适配预设** 配合完成完整链路：

1. `GENERATION_STARTED` 时先掷出十四轴 `characterCreationTicket`，同时召回与本轮输入相关的支线和 NPC 倾向。
2. 票据和召回包在正文生成前注入；Kemini 原有提示词继续负责正文。
3. `GENERATION_ENDED` 后等待 500ms，再读取最终接受正文中的人物档案回执。
4. 全批档案先解析、补齐票据、校验；任一档案不完整则零写入。
5. 完整批次被编译为标准 `UpdateVariable + JSONPatch`，一次写入 `/人物档案/byActorId` 并读回核对；不一致则尝试回滚。
6. 档案完成后，才调用酒馆当前模型推进世界支线；结果写入当前聊天的 metadata。
7. 下一回合按玩家输入、人物、地点和关键词召回相关项目，再注入正文生成。

## 仓库文件

- `index.js`：SillyTavern 事件接线、MVU事务写入、世界引擎和状态面板。
- `core.mjs`：票据、档案校验、补丁编译、世界解析与相关性召回。

预设文件受本项目隐私边界保护，不在本仓库、不在任何提交历史中，也不会由扩展联网下载。用户需要单独从本地导入适配预设。

## 安装与自测

1. 在 SillyTavern 扩展安装器中使用本仓库地址安装扩展。
2. 从本地导入单独交付的 `Kemini Dramatron 陨落的天才v1.2_MVU人物世界适配候选版.json`。
3. 保持 MagVarUpdate / MVU 正常启用；数据库、世界书和缝合怪仍按自己的方式独立工作。
4. 新建聊天测试。右下角的 🩺 面板会显示本轮档案与世界推进状态。
5. 若显示“人物档案失败”，本轮不会把半张档案算成功；展开面板查看普通中文原因。

建议测试至少覆盖：没有新人物、一个新人物、同轮多名新人物、旧人物再次出现、切换聊天、停止生成、刷新后读回、世界引擎失败后下一轮恢复。

## 明确边界

- 数据库：扩展不读取、不写入、不声称代表数据库成功。
- 预设：不上传到本仓库；本地适配版的原 47 个 prompt、原有参数、原顺序前缀和两套原正则均保留，只追加适配项。
- 医生：正文后处理人物档案和世界连续性；不负责人物首次出场的随机创作决定。
- 世界引擎：记录 NPC 的计划或尝试，不把未裁决尝试伪造成成功结果。
- 玩家自主权：脚本和追加提示均禁止替玩家决定行动、感受、同意、关系或结果。

## 本地非验收检查

安装 Node.js 后运行：

```bash
npm run check
```

这只验证 JSON、语法、原预设保持不变和核心函数，不代替真实酒馆验收。
