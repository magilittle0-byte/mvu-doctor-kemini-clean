# 0.10.30 MVU变量修复阶段验收

本阶段按用户2026-09-08明确要求验证运行链、修复按钮和失败恢复机制，不承诺生成模型没有幻觉。当前完整人物档案、世界引擎尚未实施；完整三模块真实门禁尚未完成。

同一真实酒馆新聊天完成十二个正常accepted正文回合及一个额外副本压力回合。全部回合独立核对正文、医生实际请求和返回、官方MVU、磁盘与卡片界面；多次手动修复确实读取新资料并修正遗漏、保存读回。当前源码未发现未解决的代码故障。

最后正常回合的自动候选和两次手动候选均重复包含无效删除，被官方校验拒绝，整批未保存。该回合记为失败隔离机制核验，仍为 `failed/patch_outcome`、`repairRecovered:false`。关系等语义漏项和其他模型例外保留在阶段摘要中，不能称为全部变量已经正确。前轮成功记录与本轮原生变量没有被失败候选覆盖。

最后只执行一次页面刷新。宿主先回到未选择角色的首页，首次等待恢复未成功；随后重新打开同一个原存档，没有再次刷新或生成。完整聊天、所有swipe与MVU、医生所属存储、56项卡片UI均与刷新前一致。失败状态和修复按钮恢复，正文入口可用；刷新新增模型调用为0。失败原始模型返回只在运行内存中供当前诊断使用，没有声称它在刷新后仍保留。

## 调用成本

P1每次自动检查最多1请求，自动重试0；每次主动修复最多1个新请求。本候选共58次：正文与其他扩展38次、P1自动13次、手动修复7次。普通一轮通常总4次，每点一次修复再加1次。当前充值比例下估算总1.45元，其中P1约0.50元；金额为估算，后续模块成本尚未包含。

## 锁定范围

- 运行版本0.10.30，变量模块1.0.0-candidate.31。
- 17个运行文件及兼容配置聚合指纹：`39deb6d8427e79742292d7fbf60b4e6fd9acaf7376a128770b5055c2966acb5e`。
- 当前来源提交：`d3a55f8f67c3abe0f800a5fe6fd6aacf7602ca58`；本次只新增阶段记录和文档，运行代码与提示词未变化。
- `locks/phase1.json`保护运行文件；`locks/phase1-supporting-files.json`保护相关合同和回归依赖；`locks/phase1-evidence.json`保留紧凑阶段结论与模型例外。
- 完整请求、失败、图片阅读与清理、同聊天和刷新锚点留在本地，不随仓库提交。旧验收不代替本次证据。

后续人物模块按完整正文、已确认MVU及权威设定自主设计完整档案并填表，不依赖预设专用档案标签。它必须尊重P1失败状态和数据所有权，不能把失败候选当作成功变量，也不能为接入方便改动锁定源码。
