# 0.10.30: exclude the overridden stream display default from the P1 route digest

During a normal 0.10.29 Tavern round, a read-only observer recorded four transitions of `defaults.stream_openai`, with no other route/config field changing. The digest returned to its initial value. P1 completed that round because its checks did not coincide with the transient values. Two earlier rounds had rejected paid results with `model_config_changed` and recovered through one existing repair-button request each; those earlier failure instants were not observed, so their exact changed field is not claimed as proven.

## Source and current-host evidence

- `modular/variables/module.mjs`: P1 fingerprints before and after its one model request, and before saving. It does not retry automatically.
- `modular/host.mjs`: `modelRouteHash` previously hashed the entire native completion defaults object.
- Pinned Story Oracle `callProfile` calls `ConnectionManagerRequestService.sendRequest` with `stream: false` explicitly.
- The current host's `sendRequest` passes that explicit stream value into the completion payload. `ChatCompletionService` preserves the override when building the final request.
- The current round's exact P1 native request was non-streaming. Current-host public-source SHA256 values: `callProfile` = `df115433824054f556e5da255f25f487ce6bf0377fb5b2377661aa6f7156be7d`; `sendRequest` = `11d330c7ce19afbd62a28a8efc5c9ba9e2d31037b6dab1f24094cad022e2a004`; `custom-request.js` = `21ff864cdbbec300d6a7591d25ca4bcdb17990d0f62331f59da36f0b536ea65f`.

Local evidence remains outside the install repository under `modular-cost-0129`: `seq8-config-observation.json`, `current-host-stream-source-meta.json`, and the corresponding public source snapshots. No private chat, request text, world book, credentials, or runtime anchor is included here.

## Minimal adaptation

Clone the native defaults before fingerprinting; exclude only OpenAI `stream_openai`, which this request always overrides. Do not edit native settings or request assembly. Preserve profile/model/endpoint/credential/proxy/preset and all other defaults, as well as the independent rules, schema, current/previous MVU and target guards. Keep one full P1 request, zero automatic retries, and one fresh request per repair-button click.

The regression changes the native display default while a model response is in flight. Before the fix it fails at the post-response guard with `model_config_changed`. After the fix it must save using one request and leave the externally changed native default untouched. Actual route/sampling changes must still reject, even alongside a stream-display change.

This candidate requires new real-host acceptance. The retired 0.10.29 run and its model omissions remain evidence; they do not count toward 0.10.30 acceptance. 尚未完成真实门禁。
