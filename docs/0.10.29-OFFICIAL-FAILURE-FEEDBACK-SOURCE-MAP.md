# 官方执行失败反馈的最小恢复适配

## 修改前证据与必要性

0.10.28 当前指纹 `83c8042bf90567a502ea44542510e91a6fec12c60cca55ac9e9b1b12aad9d501` 的第九轮，自动诊断和两次显式按钮分别为 native 8005、8006、8007，均重复删除当前已不存在的对象，得到 `patch_outcome`。官方校验阻止整批写入，原 MVU 保持一致。三个模型输出的语义错误不能算解析器或存储器故障。

但两次修复请求只有两条完整基础消息，没有收到上次官方失败反馈。当前目标、聊天、前后 MVU、规则、schema、配置都相同，仅完整背景 hash 变化。Root 对实际已发送背景逐行比对并读完差异，变化为八行 D4 到 D40 的动态骰表。该证据证明反馈被代码条件过滤，不证明携带反馈后模型必然成功。继续无反馈盲抽已消耗额外调用，因此适配恢复反馈传递，不编写本次故事的标准答案。

私有正文不进入本文件。仓外证据：`modular-cost-0128/seq9-failure-meta.json`、`seq9-repair-failure-meta.json`、`seq9-repair2-failure-meta.json`、`seq9-context-diff-readonly.json`。该聊天未完成十二轮，不能锁定。

## 实际读取的成熟来源

| 来源 | 可复用行为 | 本次适配 |
|---|---|---|
| 当前 `modular/variables/module.mjs` | 显式 manual、目标与快照精确绑定、动态背景只传纯结构诊断；同一完整背景才带旧失败候选 | 直接扩展到官方未执行命令的诊断，不放宽任何写入校验 |
| 当前 `modular/host.mjs` 的 `parseOfficialCandidate` | 精确绑定本次官方解析事件，返回官方未执行命令 | 只读取该收据中能与本批已解析操作完整匹配的操作类型与路径 |
| `mvu-auto-doctor-testing/actor-profile-batch-core.mjs` 1296–1347，SHA256 `278c712b7546c6204df6bf67c3820403a7d66c5f9a30eab95807919053aa9b49` | 官方失败归一成有限代码/路径，去重，作为校验反馈交回重建流程 | 复用有限诊断原则；不移植分组调用、人物 schema 或旧状态机 |
| `mvu-auto-doctor-testing/actor-profile-v6-core.mjs` 534–543，SHA256 `2c71cfd0ff98e895aa44a0056d4d610bd493354534078b807a4afcab85293b47` | 当前资料与失败反馈分开，反馈不能替代事实 | 新请求仍有完整当前背景、正文、规则和前后 MVU |
| `docs/0.9.1-VARIABLE-EVIDENCE-SOURCE-MAP.md` | 修复作用于当前 post-state，不重放原生增量；空补丁不证明正确 | 原样保留 |

此前的 `RECOVERY_CONTEXT_SOURCE_AUDIT.md` 已比较 Story Oracle、旧医生、人物修复、缝合怪、数据库等来源。本次复查具体相关源码与字节，不从数据库 CRUD 或预设语句另造恢复调度。

## 明确范围

官方返回未执行命令后，仍整批拒绝；仅额外保存无字段值的 `{op,path}`。命令必须能由 `full_match` 解析并与本批已解析操作完整匹配，不能把任意反馈字符串当可信路径。显式 manual 且原有七项稳定绑定一致时，即使背景变化，也可附带 `patch_outcome` / `patch_no_effect` 及这些失败路径。提示明确它是失败诊断，不是事实、待执行补丁或需要删除/补写的目标列表；必须重新核对当前完整状态。

不携带旧 raw、旧字段值、官方 args、自由 reason 或完整旧补丁。不删除动态背景，不缓存或冻结骰子，不增加分类器/第二模型/自动重试。每次运行仍最多一个请求；新候选仍完整通过字段范围、所有权、官方解析、命令执行、结构保护、基线、原子保存与读回。

只验证受控边界后仍必须按唯一协议在当前新指纹下重做十二轮；旧第九轮失败永久保留，不能改写为通过。此说明本身不构成可用声明。
