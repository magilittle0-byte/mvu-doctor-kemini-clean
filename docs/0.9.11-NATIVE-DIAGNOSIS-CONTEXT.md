# 0.9.11 原生诊断材料与整态审计适配

## 实际失败与对照边界

0.9.10 的真实首轮被独立核验拒绝：医生把尚未领取的物品写入背包，并把已经生效的派生加成说成遗漏。当前状态确实完整传入，并非缺少快照。人物出现过度丧失自身目标的补全，World 只复述玩家背景；这两项尚未由本修复解决，不能报告十二回合通过。

在同一宿主、同一真实模型与冻结材料上，只读对照未应用任何返回：

- 原版自动请求两次没有错误赠送物品，但对已经错误入库的状态仍返回空补丁。
- 原版整态审计配合全量叙事世界书仍误判；不能据单次空补丁宣布修复成功。
- 使用原生 MVU 规则提取，并请求原版支持的整态审计，分别检查写错前与写错后快照：不再伪报派生加成；后者识别尚未领取的物品并提出移除。两者还提出了同一时间字段修正，其完整规则适用性仍需真实回合继续核验。

以上是故障定位，不是当前候选功能验收。私人材料、模型返回和连接信息不进入仓库。

## 来源映射（修改前记录）

| 部分 | 成熟来源 | 本次处理 |
|---|---|---|
| 诊断系统提示、当前状态、更新区块、对话、卡片与输出约定 | 固定 Story Oracle 1.35.4 `buildDiagnosePromptFrom`、`DIAGNOSE_SYSTEM_PROMPT` | 原样调用；删除 Doctor 重复追加的前后态五栏和附则 |
| 变量规则提取 | 同版 `collectMvuUpdateRules` / `collectMvuRuleContents` | 原样调用；默认只供给明确标记的变量机制条目，有手选则完全尊重原生手选，没有机制条目才回退原生扫描 |
| 整态审计 | 同版 `DIAGNOSE_SYSTEM_PROMPT` 的“审计整个状态，而不只是最新一次更新”分支 | 将该原生任务范围放入一句 user 请求；不另建分析器或验证模型 |
| 无内联更新的推导 | 同版 `runAutoDiagnose` 的乙分支 | 保留原句请求与原生 auto 模式 |
| 真正写入 | 官方 MVU `parseMessage`、`replaceMvuData` | 不变，仍唯一执行器 |
| 固定楼层、证据并发、收据与读回 | 当前 Doctor 宿主适配 | 不变；前态仍用于事务有效性核对，不再重复灌入模型提示 |

本次新写内容仅为上述选料顺序与原生任务范围的宿主连接，不新增 schema、状态机、语义门禁或模型调用。人物补档、World、数据库、预设的所有权不变。私人预设不改、不上传。版本只用于已授权 Kemini Clean 测试 main，尚未完成真实门禁。

## 随后真实复测：未通过

2026-09-05，准确安装并重载本版本后，在全新聊天使用既定预设、内嵌世界书及当前真实配置完成首轮，独立核验拒绝，未发送第二回合。正文约 54 秒，Doctor 约 92 秒，原生 World 约 244 秒；耗时未作为失败依据。

- 变量：模型修改了机制规则明确由前端托管的经验需求值，固定楼层读回确认错误值真实落库；正文说明尚待领取的物品仍留在背包，未被纠正。
- 人物：本轮在场人物完整提交一张档案，持久化和桌面面板展示正常。本轮未重复出现上一候选的离场人物人格丢失案例，不能把上一候选的个案直接算成本轮失败。
- 世界：原生解析、持久化及下一轮注入准备完成，但只增加静态势力资料和旧背景记录，没有新的主体行动。第二回合未运行，因此没有下一回合实际消费证据。
- 另外两次冻结输入的只读对照改用原版自动诊断请求，也出现误判或漏检；未执行其返回。不能把替换回原句直接宣称为已修复，更不能把当前问题完全归因于附加措辞。

本版本基础检查 20 项、Doctor 浏览器回归 120 项通过，但均不替代真实验收。十二回合有效通过数仍为 0。后续须围绕上述真实输入、返回与写回继续定位，而不是增加格式门禁或凭单次对照继续换提示词。
