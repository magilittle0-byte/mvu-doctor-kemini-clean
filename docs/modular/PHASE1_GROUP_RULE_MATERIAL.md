# 第一阶段：分组字段规则原文

2026-09-08，运行修改前来源映射。0.10.16 第三轮的任务误写已拒绝。完整阅读实际命中的世界书条目后，确认其中是数据库表投影；其任务行定义不等于 MVU 当前副本容器已满足初始化条件。仅补充两处作用域说明的只读草稿仍误写，已撤回，未推送。

## 本次重新查阅的完整机制

- 全项目与历史重新检索分组填表、目标表规则、YAML及字段声明；不复用旧检查的通过结论。
- 数据库 spv8.4：`prepareAIInput_ACU`（23033–23280）按目标表键拼接表说明、全部初始化/增删改条件和当前行；`compatibilityKey_ACU/buildGroup_ACU`（44818–44910）只负责兼容参数和组身份；`collectGroupFillResponse_ACU`（45348–45435）先构造目标表材料再调用模型；统一写入（45571–45822）及组调用方（45979–46108）检查目标互斥并统一提交。这些相关实现与调用由 Luna 完整读取。
- 当前 `planVariableGroups/checkGroupScope/groupInstruction`、变量模块发送和官方提交链，及官方 MVU `util/common.ts` 的 `parseString`、额外模型的 `parseCustomIncludeBody` 已读取。旧 0.10.9 的嵌套字段探针仅作为本机只读调用和保全现场的代码来源，不作为本候选验收。

## 最小适配

| 内容 | 来源 | 处理 |
| --- | --- | --- |
| 每组目标表自己的说明、更新触发和当前行紧邻展示 | 数据库 `prepareAIInput_ACU(targetSheetKeys)` | 最小适配为 `group.paths` 对应完整字段声明原文及写前 MVU 值 |
| 字段声明边界与路径展开 | 当前分组规划 `planVariableGroups`、`expandPath` | 复用既有格式和路径规则，取完整原文段，保留嵌套 check、多行 type 和父级约束，不解析剧情语义 |
| 原生模型、全量背景/历史/规则、范围校验及统一官方写入 | Story Oracle 原件及既有变量模块 | 保持原链不变；不删除原始材料，也不增加另一个模型或执行器 |
| 目标路径到相关声明段的选择与展示 | 数据库表键不兼容 MVU 点路径和父子容器 | 必要新写的展示接缝，祖先/后代声明均保留；没有匹配段时仍依完整原规则检查，不能声称没有规则 |

该选择器只取当前分组规划已识别的字段声明段，不把正则当 YAML 语义解析器。嵌套字段全部保留在段内；非该声明格式的规则仍保留在原完整输入。这里无需将原文转换成 YAML 对象再重序列化，也不增加解析依赖。

只读定位使用当前失败第一组的原始材料与同一模型，仅追加八个目标声明段和其写前值。17726ms 后明确识别未进入副本，返回空补丁；完整输出在本机核对，聊天、MVU和设置未变，未执行补丁，临时图已清理。这仅支持采用展示适配，不能证明运行可用。

失败的来源说明草稿不进入生产。新的运行候选只增加组内原规则材料，其余语义提示、初始化能力、预设、数据库配置、历史读取和持久化保持原有流程。必须按新指纹从全新聊天完成十二轮，未完成前不锁定；人物档案和世界引擎仍未开始。
