# 0.10.20 nested insert loss: source map before edits

2026-09-08. Real seq6 (normal round5) rejected after full input/body/MVU/31-group output/UI/native disk review. The accepted narrative and Doctor insert both carry a title; the card Zod expects a different NPC title object, silently strips the supplied nested title keys and prefaults the title to its empty default. All four official commands are consumed. This is a structured-data loss, not an API failure or time rejection.

The complete current card schema source (14945 characters, 13 private images) was read locally and cleaned. NPC and teammate title use a flat name/effects object; the player uses a current/backup title container. The current Doctor passes field rules and stat_data but not this source declaration; its before.schema is only the official sentinel string. No private names, title values or card source are copied here.

A fresh project-wide filename/keyword search covered official MVU, old Doctors, database/templates, Story Oracle, the reference archives, cards/frontends, Izumi, sugar-company, planner/forum and diagnostic/review files. Relevant source implementations were read, including their callers:

- Actual loaded-mvu-zod.js, complete registerMvuSchema: official safeParse remains sole interpreter; objects may strip unknown keys, prefault missing keys and normalize scalar values. Full current card schema confirms the concrete loss.
- Official update_variables.ts COMMAND_PARSED -> for_zod -> ended_for_zod; current host.mjs nonce-bound receipt and module.mjs full commit/retry path remain unchanged in ownership.
- TavernHelper sourcemap src/function/script.ts getScriptTrees, src/type/scripts.ts, src/store/scripts.ts and settings/character.ts: reuse the public current-character script tree getter and exact enabled tree/folder rules. src/store/variable_schemas.ts and variable-manager consumers have no public schema getter. Do not monkey-patch Helper or reconstruct its private Zod store.
- Story Oracle buildDiagnosePromptFrom and current modular composeDiagnosisMessages: reuse native field/world/current-state source assembly; add the full enabled embedded MVU schema declaration as read-only structure material, never execute it or invent a card-specific schema. It is source material, not current story facts or a Doctor command.
- Old Doctor core.mjs deepSubset (88-100), simulateOps (427-501), validatePatchResult (832 onward), plus legacy path snapshots: reuse recursive requested-child checking only. Do NOT transplant its value equality or local expected-state executor: prior 0.9.7 and phase1 outcome work proved those reject valid normalization.
- Existing current group cache/feedback/official parse/atomic save/readback: keep. Missing object keys on an independent literal insert/replace feed the original offending group retry with its raw request and the actual uncommitted official subtree. Other groups remain cached. Default fields, scalar coercion and derived value changes remain official-owned. Overlapping or index-shifting sequences are left to official receipts and real review, not guessed by another interpreter.

New glue is limited to selecting active embedded schema source, hashing it against stale/recovery receipts, recursive lost-key evidence for unambiguous object targets, and routing that structural feedback through the existing retry. No preset or native card changes, no new semantic auditor, no second MVU writer, no phase2/3 implementation. No success claim before new-source real 12-round acceptance.

Frozen single-group diagnosis: adding only the exact production schema-material section to the unchanged original messages returned two inserts in 74720ms. Official cloned parse consumed both, preserved the title and reported no lost object keys; the new structural check detects both malformed objects in the original failed output. Full output and unsaved candidate were read locally and captures cleaned. Live MVU remained unchanged; no save occurred. This is a diagnostic, not a real acceptance round. npm test 99/99 and current 17-file syntax/JSON checks passed before installation.
