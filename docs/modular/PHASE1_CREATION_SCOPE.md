# 第一阶段：创建登记不能免除其它字段的条件

0.10.22 真实同聊第四轮已拒绝，正常通过3/12。已接受正文只使用既有背景能力，医生初次与自动重试均将其写成新的正式技能，并自行给出等级、消耗及范围。最终34条操作被官方消费，变量、界面和原生磁盘都包含错误；执行成功不能证明内容正确。没有继续失败聊天或把旧轮次计入新候选。

## 修改前的真实路径与来源

完整核对原始输入、最终正文、两次实际模型消息和返回、当前原卡全部字段规则、官方解析、存档和前端。相关世界书条目确实出现在当前请求中；没有规则丢失、模型替换或截断证据。原卡对创建字段、能力取得、升级及任务初始化分别规定条件，背景能力事实本身不提供新数值定义的权限。

本次重新搜索全项目与历史参考，读取 Story Oracle 的完整诊断提示、构造及模型调用；读取数据库 spv8.4 的默认分段提示、主角信息表、技能表、prepareAIInput_ACU、callCustomOpenAI_ACU 和 collectGroupFillResponse_ACU。数据库按表分别描述初始化和增删改条件，可沿用其权限边界；数据库自己允许初始技能填表，不能把这项权限复制为当前 MVU 卡的规则。也核对旧医生的身份来源匹配及失败反馈链，未找到可直接移植的通用变量语义检查器。

0.10.16 的原修复是防止模板默认身份挡住用户明确创建值。真实失败输出却把通用的“保护创建设定”当成其它字段原 check 的例外。规则引用反馈已经使模型引用正确原句，它仍作出上述错误解释，因此没有添加无效的规则引用校验层。

## 最终改动

| 部分 | 来源 | 处理 |
| --- | --- | --- |
| 完整世界、历史、字段规则、模型连接及官方 MVU 执行 | Story Oracle 与现有模块 | 原样保留 |
| 创建登记与其它字段触发分开 | 本卡 check、数据库主角表/技能表操作条件、0.10.16 创建适配 | 最小修改现有 EVIDENCE 身份段与 groupInstruction 同一句权限说明 |
| 给定事实与自行设计数值定义的区别 | 本次真实失败及原规则 | 新写必要的自然语言解释；不增加字段名单、能力关键词、模型调用或状态机 |

原 check 规定创建时设定的字段直接登记用户给定值，显式更正和无需正文重复的权限保留。其它字段分别满足自身原 check；规则明确授权且已触发的初始化仍须完整设计。背景事实继续保留，不因没有对应正式数值条目而否认。未改预设、独立数据库、系统层初始化说明、分组结构、解析、保存或恢复。

## 验证界限

冻结失败的初次请求和实际重试请求各调用一次当前配置模型，仅替换上述同一权限的两处表达。两份完整返回均保留背景能力事实并给出零补丁；实际出站消息和返回逐字匹配。聊天、MVU、设置不变，无执行无保存。这是候选依据，不是真实功能验收，也不能放行旧聊天。

未修改既有测试断言。新候选仍须安装当前指纹并从全新聊天完成十二轮、逐回合独立检查及最终刷新读回。变量未锁定，人物档案与世界引擎尚未制作；不得据此宣称已经可用。
