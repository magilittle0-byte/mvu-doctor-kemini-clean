# 第一阶段：区分世界设定、字段合同和当前状态

2026-09-06修改前记录。冻结0.10.6完整请求的消息角色、核对措辞、末条状态、原操作隔离等定位都未整体通过；仅规则范围的定位20.525秒识别了未领取装备，但仍漏点数与关系，并误报已存在字段。最小合成场景能正确完成三种修复，不能将这些定位或旧0.9.11视为可用证据。

本轮再搜索完整参考目录、数据库spv8.4、Story Oracle及旧医生的背景/规则/状态消息组装；完整读取数据库DEFAULT_CHAR_CARD_PROMPT_ACU、callCustomOpenAI_ACU，Story的DIAGNOSE_SYSTEM_PROMPT、buildDiagnosePromptFrom、buildCardSection、buildTranscript/Turns、stripMechanismBlocks、runAutoDiagnose和callDirect。本次不再增加另一套模型或执行器。

确定的接入偏差：模块将完整世界书（游戏设定和多种生成模板）与补回的MVU条目拼成同一wiBlock，原生builder把整块命名为“角色卡MVU规则”。字段路径/type/check并没有独立的资料边界。原生DIAGNOSE本身定义字段规则的所有权，而完整世界资料还包含其它用途，不能都升格为变量字段结构。多轮类型误报的内部原因仍不能确定；本次修正的是输入职责，并须重新测试实际效果。

复用映射：
- 原生DIAGNOSE职责/输出合同、getSettings、模型调用、补丁提取、官方MVU解析与事务不变。
- 原生buildWorldInfo + collectMvuUpdateRules保留全部既有世界取材，精选行为不变；独立rules仍用原生收集结果。
- 原生buildCardSection保留完整人物设定；buildTranscript以原生keepMechanism=false保留历史正文，原始增量仍在原始聊天和本轮记录。当前正文已独立固定提供，历史只排除这一个重复的当前助手楼层，其余历史沿用原生深度。
- 数据库模板的背景资料段与当前表格段最小适配：system只放医生职责和全局附加提示，user先给完整世界/人物背景，再给完整历史/本轮正文与输入、原更新记录、明确字段规则、前端所有权、前态与当前态，最后给已有唯一核对任务。背景中的故事生成/格式指令不当作医生指令，固定世界事实与游戏规则仍有效。

新写仅纯消息组装函数，因为原生buildDiagnosePromptFrom不能把整块世界观和字段合同放入两个独立资料槽；仍调用其原有卡片/历史子函数，不新建上下文扫描器。当前与前态JSON恢复原版缩进；已解析状态放在任务前，原操作仅保留一处且明确不是当前字段结构。全局自定义入口仍唯一，保持在system职责层。

此方案连同本次已确定的权限主语修正先做针对性检查及冻结真实材料只读定位；不执行对照补丁。不因单次结果合理而锁定，最终仍须当前候选全新聊天十二轮验收。
