# 第一阶段：完整规则核对的输出与遗漏恢复

状态更正：下文是已撤回草稿的修改前映射。只读定位返回 52 个齐全编号，却仍漏检真实关系与区域时间；编号覆盖不能证明语义覆盖。core/module/prompt/test 的覆盖草稿已撤回，补丁仅在本机 run-state 留存。保留的两处界面说明修改只是区分模型修复与官方派生更新。后续数据库消息分段和初始化约束对照也未完整修好，均没有写入生产提示词。

2026-09-06，修改前来源映射。0.10.7 首轮已经独立拒绝，0/12；没有继续生成。真实请求与回包逐条内容核对，模型收到完整规则、当前正文和关系补写要求，但仅返回已有属性、天赋和库存的总结，遗漏已触发的空容器。空补丁进入官方管线后发生派生计算并成功保存，不能据此证明全部字段检查正确。

已重新全项目检索旧医生、参考归档、数据库本体/模板、糖糖、Izumi、缝合怪、Z论坛、角色卡/规则/前端和报告。完整阅读原版 Story Oracle DIAGNOSE_SYSTEM_PROMPT 及当前模块输入、解析、重试、保存、诊断界面链；完整重读数据库 spv8.4 DEFAULT_CHAR_CARD_PROMPT_ACU（1267–1313）和 callCustomOpenAI_ACU（23498–23764）。

来源与边界：

- 原样保留 Story Oracle 的取材、当前连接、单一 UpdateVariable/JSONPatch 及官方 MVU 执行入口；不增加第二个语义审查模型。
- 最小适配数据库“先读全部 note，再判定各表/字段变化”的流程为逐 check 输出短核对结论。保留所有规则和背景；不给模型塞特定人物、私有场景答案或固定关系数值。
- 新写必要的结构覆盖检查：在规则文本的每个 check 声明旁添加仅供本次核对的编号；返回 Analysis 按编号给出简短触发依据及当前值/处理。脚本只检查编号是否完整、重复或缺失，不解释普通正文、不用关键词判断人物或成功，不把编号齐全视为语义正确。没有 check 声明的规则用当前状态分组作为回退核对目录，仍保留原规则。
- 缺项复用当前模块已有的失败反馈和有限重试；未完整核对不进入官方 parse/写入。无需另造调度器、补丁执行器、人物发现模块或嵌套 JSON 协议。格式故障与语义验收继续区分。
- 当前参考仅有自然语言“全部阅读”要求，没有可直接移植的 MVU check 输出覆盖检查。旧人物覆盖器绑定 Actor/身份票据，接口及状态所有权不同，不能带入第一阶段；因此只新写上述无语义结构检查。
- 最小修正界面：空模型补丁引发的官方派生更新与非空模型修复分别描述；存档读回仍只报告持久化结果，不冒称语义已验收。

先验证遗漏输出不能写入且重试沿用原事务；再对被冻结失败材料做一次相同原生连接的只读定位，返回只留本机、不执行补丁、不算验收。通过仅支持进入新候选，最终锁定仍须新聊天十二轮真实独立核验。当前模型路由指纹范围的潜在缺口单独保留待查，不夹带未证实修复。
