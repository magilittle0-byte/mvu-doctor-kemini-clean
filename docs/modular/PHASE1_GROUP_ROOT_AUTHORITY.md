# 第一阶段：顶层分组必须有结构来源

2026-09-06，0.10.8 真实首轮已拒绝，0/12。修改前来源映射。

实际链路：九组模型检查185.171秒，三项补丁及官方派生计算已经保存；完整正文、返回、变量、变化和规则共36页在本机核对，独立宿主存档读回一致。三项补丁未发现误改，但65个分组范围中有1个来自规则的操作说明标题，并非变量字段。该路径没有被写入；本项是分组适配错误，不能称为变量污染已发生。实际卡片前端截图被宿主抽屉遮挡，未据此宣称前端验收通过。

重新搜索了项目及历史的schema、字段路径和数据库targetSheetKeys。完整读取官方MVU的generateSchema、getSchemaForPath、reconcileAndApplySchema及ObjectSchemaNode/MvuData定义，重读数据库prepareAIInput_ACU按真实tableIndexes/targetSheetKeys取表的调用部分和当前分组实现。实际宿主此卡由Zod接管，payload.schema没有properties，不能假称取得完整官方字段schema。

- 原样保留前后态变量键、完整点分路径声明和已有组内全部后代范围；不修改字段check、正文或原生发送/执行。
- 最小适配数据库“目标键来自实际结构”的边界：单段顶层声明只在前态、当前态或官方object schema.properties明确包含该键时入组；不能因为下方有check就认定它是变量。
- 官方schema直接来自已经读取的前后态MvuData；只取type=object的properties键，不从字符串标记或普通叙述猜schema，不执行schema生成/清理，不篡改官方状态。
- 新写可选schema参数接线。已缺失但官方schema明确声明的顶层字段仍需核对。此前合成用例只给一个含check的孤立标题，缺少“它确实是字段”的结构来源；补上真实接口形状的schema输入，保留原缺失字段与整容器可修复断言，另加操作说明排除用例。不是降低修复标准或把空字段算成功。

受控检查后核对真实冻结材料的64条范围全部有效；不再额外调用模型定位。冻结0.10.9后从新聊天重做十二轮，不累计0.10.8。
