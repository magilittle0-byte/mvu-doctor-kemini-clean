# 第一阶段：分组基线的规则变化与结构覆盖

2026-09-06，修改前来源映射。当前真实验收仍为 0/12。

重新全项目检索了规则指纹、baseRevision、旧医生、数据库及历史参考文件。完整阅读当前变量模块和分组规划器，以及数据库 spv8.4 的运行版本快照、assertRuntimeRevisionFresh_ACU（11440起）、runTableWriteTransaction_ACU（11669起）及其已读的分组收集和统一提交调用链。

- 已确认代码路径缺口：ruleHash 只在开始和已完成收据复核时检查，assertBaseline 未重新读取规则；模型等待期间规则改变后，旧候选可能仍进入官方解析或提交。先用现有合成 harness 复现，再把现有 readRules/digest 接入原守卫。适配数据库在异步工作后、提交前检查同一基线的机制，不新增事务管理器。
- 原样保留 Story 的规则选择/收集，以及动态世界上下文与稳定字段规则的分工；不把每次动态扫描文本当作稳定版本。原规则无法读取或摘要变化时，整轮旧结果作废，不在旧输入上重试。
- 分组器仍复用现有 dotted/${a|b} 声明和 JSON Pointer。补查两个结构边界：有 check 的顶层字段虽前后态都缺失仍须入组；被错误标量占据的父容器须整组拥有，才能通过官方 MVU 修复容器，不允许多个组分别替换同一父容器。这是 MVU 嵌套路径适配，数据库平面 sheetKey 不提供可直接移植的代码。
- 先增加有实际结果断言的受控复现；通过后仍必须冻结新候选，在真实酒馆从新聊天完成十二轮。受控检查不构成验收，不修改原字段含义、模型配置或预设。
