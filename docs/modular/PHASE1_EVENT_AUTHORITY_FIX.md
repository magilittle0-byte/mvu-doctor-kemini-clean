# 第一阶段：事件条件与写入结果分开核对

修改前来源核对：2026-09-06；基线79a007b、0.10.0，运行指纹5d87278e84e029026a5a64c02d1dcd4e1b94272879ba3be33a7c66722e70d330。

真实首轮主模型49.366秒、变量医生20.314秒。医生认出了物品尚未交付，却把空背包判为漏写，补丁新增了可用物品。独立宿主存档读回与页面及候选一致，说明这是已经持久化的错误判断，不能用applied或readback判通过。该聊天批准0轮，停止后续生成，未锁定。

本次重新全局检索旧医生、故事神谕、数据库模板、糖糖、Izumi、缝合怪、Z论坛和历史报告的相关路径。已完整读过当前模块及其宿主读写、原生DIAGNOSE_SYSTEM_PROMPT、resolveModePrompt、buildDiagnosePromptFrom、buildCardSection、buildTranscriptTurns/buildTranscript、stripMechanismBlocks，旧runStoryDiagnosis的证据与单次调用段。现场模型返回、对应完整背包/装备规则和实际前后态在本机核对，不复制私人原文。

| 来源与位置 | 复用或适配 |
| --- | --- |
| Story Oracle 1.35.4的DIAGNOSE_SYSTEM_PROMPT | 保留角色规则、实际状态、类型、路径、最小纠正和唯一JSONPatch输出；替换“执行生效即无错、已存在条目不得判错”的整段，明确执行结果仍需接受剧情核对 |
| 同版resolveModePrompt及diagnoseSystemPrompt入口 | 直接使用原生覆盖入口，在本次调用的设置副本中适配，不改vendor、不改用户保存的设置、不创建第二诊断器 |
| 同版buildDiagnosePromptFrom及原生上下文构建 | 原样调用，仍给出规则、当前状态、原更新、角色和历史；原有前态与精确最终正文证据保留 |
| 数据库表格模板的“仅镜像已经确认终态”、忽略未选分支规则 | 语义原则最小适配到变量：字段结构要求不能充当事件已发生的证据。数据库自己的最新MVU优先规则不移植，因为医生的任务恰是审计MVU |
| 旧0.9.9/0.9.11诊断与真实失败报告 | 作为失败证据，不作为已验证实现；不重复仅在冲突旧规则后追加另一段否定的做法 |
| 官方MVU parseMessage、replaceMvuData与宿主存档读回 | 原链保持，仍一次官方解析并固定楼层提交；不按“领取”等自由正文关键词硬编码物品裁决 |

新增部分仅为提示合同适配函数及其实际原生提示的受控检查。先核对事件的触发条件，再判断字段应有值；初始化、条件承诺、待领物和已获物分开。可用背包不能自行改称待领账册；明确已发生的获得也不得因保守而漏写。不会增设模型或人工运行批准步骤。提示修改后的旧真实证据失效，必须更新候选并从全新聊天重新验收。
