# 第一阶段：规则授权的初始化与事实登记分开

2026-09-06，修改前来源映射。0.10.9 在真实新聊天第一轮通过后，第二轮因漏检嵌套初始化字段而被独立拒绝。当前没有模块完成真实验收或锁定；旧轮次不得累计到新候选。

## 实际问题

字段规则明确要求触发后初始化一组条目，当前状态只有部分条目。医生实际返回却把尚未在正文逐条展开的初始定义理解为“不得编造”，判定无需补写。官方写入、存档和界面均保留缺项。问题发生在模型诊断，不是保存失败。

完整原始正文、模型返回、实际system、当前MVU及受影响界面已在本机核对；私人文本不进入此文档。针对同一冻结输入的三次只读定位显示：仅展开嵌套规则仍遗漏；展开规则并澄清初始化边界后补齐；最后直接用原分组消息、仅替换这一条system约束，也得到必要初始化补丁。三个结果均未执行，不是新候选真实验收。

## 来源与最小适配

- 已重新检索项目与历史参考，包括旧医生、数据库及表格模板、故事神谕、糖糖、Izumi、缝合怪、Z论坛、角色卡规则与诊断资料。
- 原样保留 Story Oracle 1.35.4 `DIAGNOSE_SYSTEM_PROMPT` 的输出协议，以及原生请求、完整背景取材、MVU官方解析、分组所有权、统一提交和存档读回。相关完整函数本轮已读：`DIAGNOSE_SYSTEM_PROMPT`、`buildDiagnosePromptFrom`、当前 `composeDiagnosisMessages` 和完整模块运行链。
- 直接参考数据库 spv8.4 完整 `questsEventsSheet`、`optionsSheet` 和 `prepareAIInput_ACU`、`collectGroupFillResponse_ACU`：初始化生成定义与已发生事件登记有不同触发条件。任务定义与玩家完成任务不是同一件事。
- 仅在现有 `adaptDiagnosisPrompt` 中最小替换原版一条“不得添加文本没有的细节”的约束：禁止编造已发生事实；但当本卡check明确授权、且前提已发生时，应完整生成初始定义，保持规定的初始状态与权威设定。没有初始化授权的字段不扩大创作，不替玩家行动，不提前兑现。
- 这段边界文字需要新写，因为Story原版以保守修补为目标，并未区分本项目的规则授权初始化。没有改vendor，没有新增角色卡专用数量、名称、任务模板或第二个世界推进器。
- 没有把试验用的YAML规则展开、结构遍历或额外模型调用引入生产：只改一条指令已经得到当前定位所需结果，避免扩大运行改动。

## 验证与边界

现有真实消息组装检查要继续核对原版输出协议的全部未改内容；只对明确替换的一条约束更新预期，并检查事实保护及初始化条件同时存在。不能把这项受控检查当成模型正确性证明。

随后更新候选版本、语法/JSON与相关快速回归、核对完整差异及凭据模式，再按常驻授权推送唯一Kemini Clean安装仓，安装后新聊天从第一轮重新执行真实验收。人物档案与世界引擎仍未制作；MVU自身规则要求的初始化变量不冒充后两个模块已经完成。
