# 第一阶段：在原生末条任务中核对事件完成条件

2026-09-06，修改前来源核对。0.10.4真实首轮医生26.774秒、2项操作，官方派生计算和独立存档读回均生效；但医生把正文明确尚待领取的装备从装备栏移入可用背包。完整正文、医生说明、全部状态差异与存档已本机核对，首轮拒绝，0/12，不继续旧聊天生成。

## 来源与兼容性

重新检索项目中的数据库、糖糖、Izumi、缝合怪、故事神域及医生来源映射。沿当前选用的Story Oracle 1.35.4完整读取DIAGNOSE_SYSTEM_PROMPT、runAutoDiagnose、autoApplyFix、refreshMessageBar、buildDiagnosePromptFrom和本模块的请求、重试、解析、写入链。原版已使用“系统上下文＋最后一条用户诊断任务”，没有必要再造执行器。数据库填表和其他创作预设不承担这里的物品交付裁决，不能替换原生诊断链。

当前模块把证据合同追加在系统提示开头、完整世界上下文之前，最后的用户消息仅泛称核对状态。真实医生说明已经区分未领取，却仍把约定归属当成可用库存；这不是JSON、路径或保存错误。指令位置是否改善模型行为必须由新候选实测，不能把它当成已证明的唯一模型根因。

## 最小适配

- 复用原版system/user两条消息、完整世界/卡片/历史、输出格式、模型路由与官方单次解析。
- 将现有唯一证据合同移到原生末条user任务，不在系统开头和末尾重复堆叠；原生系统提示中先前的事实权威冲突替换仍保留。
- 在同一任务中明确：存在、约定归属、实际交付并非同一个状态；可用背包或装备代表已经取得，不能通过换容器保留一次提前发放。已经持有的物品依据前态和正文保留；真正交付后须完整补入，不能因保守而漏写。
- Analysis只需简短写实际缺陷、对应已完成事件及正确值；不加结构化语义审查层，不写场景专用物品名，不改卡片、不改预设、不更换模型。
- 受控测试核对最终发送消息顺序、证据合同恰好一次、原生输出契约和保存配置不变，其他解析、权限、来源加成及持久化断言保留。受控通过不证明真实可用。

0.10.5必须推送安装当前指纹后，从全新聊天重新进行真实十二回合；变量未锁定，人物和世界模块仍不接入。
