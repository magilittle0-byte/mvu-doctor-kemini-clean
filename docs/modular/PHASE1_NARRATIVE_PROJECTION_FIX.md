# 第一阶段：原生正文投影与派生加成来源

2026-09-06修改前核对。0.10.1（41b2a3a）真实首轮再次被拒绝，批准0轮：字段权限校验挡住了首次直接改写派生总值，模型重试却将同一天赋另记到自定义加成，造成重复计算；同时把未领取装备从装备槽移入可用背包。医生44.212秒，独立宿主存档读回成立。上一版提示合同确实已发送，不能将其称为已修复。

## 重新检索与完整实现阅读

重新检索整个项目及历史中的旧医生、故事神谕、数据库模板、世界过滤、糖糖/Izumi/缝合怪/Z论坛、变量与前端文件及报告。完整读Story Oracle 1.35.4的buildTranscriptTurns、buildTranscript、messageVisibleForTranscript、stripMechanismBlocks、buildDiagnosePromptFrom，旧Doctor的filterMvuMechanismBlocks/worldFilteredDialogue及World filterDialogue上下游；当前模块的host.contextSnapshot、run、权限失败重试与写后读回也已读完。

现场只读取证确认：原生Story已开启当前正则，当前回复投影没有规划/选项；模块在该原生上下文之后又追加未经处理的target.content，重新引入这些非事实块。因此此次不编写新的剧情过滤器，直接调用原版buildTranscriptTurns对精确当前助手行投影，keepMechanism=false仍使用原版stripMechanismBlocks。原始消息继续绑定身份、swipe和原更新，不删聊天内容，不更改预设正则，历史对话深度与原生行为保留。

本机完整阅读当前角色卡前端applyAutoLevelAndDerivedStats及init事件注册与背包装备处理链。前端自动把天赋、职业、装备及自定义加成相加；背包中完整装备可直接穿戴。代码原文不进入仓库。真实前态的派生总值落后新天赋源字段，并不能证明天赋没有被自动处理；把同一来源抄到自定义加成会在官方解析事件中重复计入。暂不能仅凭该次快照判定具体宿主事件时序根因。

## 最小适配

| 路径 | 来源与边界 |
| --- | --- |
| 当前正文取材 | 直接调用原生buildTranscriptTurns，输入仅精确当前助手行、深度1；这是当前回复单独证据，不裁剪原生完整历史 |
| 输出与持久化 | 保留官方MVU唯一候选解析和精确楼层保存，未添加本地派生值计算器、空补丁预执行或第二模型 |
| 只读字段重试 | 在既有定向修复反馈中明确不能通过改源字段绕过派生字段权限；源字段有独立正文/规则错误时仍应修正 |
| 加成语义 | 依据实际前端的来源相加机制，最小补充“同一来源仅记一处、不得用自定义来源补偿前端总值”的合同；不硬编码私人天赋名称、路径或数值，不封禁合法的新自定义加成 |
| 检查 | 受控检查验证调用原生投影、当前行绑定、原始消息不变及投影失败不发送；真实语义仍须新指纹全新聊天重新验收 |

0.10.1的记录仅为失败证据。尚未锁定变量模块，不开始人物或世界模块。
