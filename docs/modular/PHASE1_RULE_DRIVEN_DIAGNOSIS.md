# 第一阶段：按字段规则核对，恢复原生可读状态输入

2026-09-06修改前来源记录。0.10.6已拒绝，角色分离只读对照同样失败；不采纳角色分离，不把该对照当验收。实际最终正文6101字符已完整本机阅读，其中check是已展示的检定记录，HTML是天赋/装备说明，并非发现了隐藏规划；保留其信息，不按猜测删除。

重新搜索旧医生、数据库本体/模板、参考归档、糖糖、Izumi、缝合怪和Story Oracle的逐字段/逐表规则核对与输入组织。完整读取数据库spv8.4的DEFAULT_CHAR_CARD_PROMPT_ACU（1267–1313）及callCustomOpenAI_ACU（23494–23767）的消息组装和调用；原版Story Oracle DIAGNOSE_SYSTEM_PROMPT、runAutoDiagnose、buildDiagnosePromptFrom、buildTranscriptTurns完整已读。

实际偏差：当前模块虽在末条要求整态核对，原生系统任务第一项仍要求“逐项核对最新更新...说明是否正确生效”，返回反复围绕已有操作，遗漏完全未更新的字段。原版本就支持用户选择整个状态审计，本次明确替换这个范围句，而不是继续叠加另一段提醒。数据库成熟流程是先读各表note、据规则判断哪些字段需写，采用此核对顺序最小适配成卡片check，不复用SQL执行或其多段套话。

第二项适配回退：原版runAutoDiagnose以JSON.stringify(stat, null, 2)提供可读层级。当前模块擅自改为紧凑单行JSON；实际模型把真实object误判成string。恢复原生缩进，对前态也使用同样表达；只改空白，不删除任何状态和上下文，不宣称这已证明模型误判根因。

保留：原生系统的合法路径、类型、最小残差、单一UpdateVariable格式、全世界上下文、完整历史、精确当前正文、唯一末条任务、路由与事务。最小新增：默认任务第一项按字段规则给简短核验结论、明确触发状态；自定义非原生提示不被覆盖。没有新模型、第二套状态机、字段语义硬编码或全局审查层。

先对已冻结失败材料做一次同路线只读诊断，全部回包只在本机读，不执行补丁、不作为验收；若仍失败就保留失败结论。候选最终仍须新版本、全新聊天、十二轮真实核验后才能锁定。
