# 第一阶段：按实际交互核对关系触发

2026-09-07 修改前来源映射。0.10.14 配套 0.10.14r1 的第二次真实聊天首轮已拒绝：正文中的稳定称谓对象针对玩家问题作出回应，关系字段规则要求在结识时登记；Doctor 已读到对象，却以系统投影、没有实质互动为由保持空容器。官方三项其他操作全部执行，完整 MVU、实际面板和原生磁盘一致，不能抵消此项漏检。未进行下一轮，不跨聊天累计。此前 TLS 失败已在原连接恢复，与本次规则解释缺陷分开记录。

## 重新检索与实际读取

- 全项目及参考归档检索人际关系、交互、角色发现和登记规则，覆盖旧医生、Story Oracle、数据库与模板、LifeState、NPC Tracker、糖糖、Izumi、缝合怪及角色卡资料；只把直接相关的实现作为本次来源。
- Story Oracle 1.35.4 `index.js` 的完整 `DIAGNOSE_SYSTEM_PROMPT`、`buildDiagnosePromptFrom`：本卡字段规则决定约束，实际对话决定应有状态；最小补丁进入官方 MVU。当前适配和模块从输入组装、分组请求、官方执行收据到持久化的完整链路已对照。
- 同一原件 `BUILDER_FORGE_PROMPT` 中完整“人际关系”工艺块（429–433 行）：以定位和具体交互为依据，系统、脑内声音等实体也能有可观察的来回回应。读取对应模板选择和构建调用；它原本用于人物草稿，不是 MVU 关系规则。
- 旧 `profile-engine.js` 的 `discoveryPrompt`、`normalizeDiscoveryResponse`、`discoveryNamesVisibleInAcceptedReply` 与调用上下文：最终正文稳定称谓、可见身份绑定和纯标签排除分开处理。该旧业务未通过完整验收，仅作为边界参考，不将其人物发现器接入第一阶段。
- 本轮实际规则、完整正文、全部模型输出与状态在本机核对。天赋总加成另与背景中的完整天赋条目核对，属于允许范围，未将其误列为缺陷。系统实体也不能凭类别自动认定应有好感变化。

## 来源到实现

| 部分 | 来源 | 处理 |
| --- | --- | --- |
| 字段定义、触发条件、最小差额补丁 | 原版 Story Oracle 诊断及本卡规则 | 保留原有接入和语义所有权 |
| 具体交互而非抽象重要性 | Story Oracle 人际关系工艺块 | 最小适配为已发生交互的判据；不复制人物草稿的创作权限、重要他人筛选或整张档案模板 |
| 正文稳定称谓与纯通知区分 | 原件交互例子、旧发现器边界 | 只改现有唯一变量任务的一段说明；不移植标签词表或另建人物发现流程 |
| 关系登记与好感变化分开 | 当前关系字段的独立状态含义 | 新写最小说明，防止将登记误作赠送好感或亲密关系；数值仍由本卡规则和实际行为决定 |

本次只替换 `EVIDENCE_INSTRUCTION` 原有称谓段，明确不能自行提高本卡触发门槛，系统形态不是豁免依据，纯机制通知也不能被虚构成人物。没有新增模型、语义判定器、路径黑名单、表结构、存储或第二阶段功能。预设、数据库、MVU 官方执行、历史输入、分组、恢复和锁定流程不变。

快速检查仅证明接入和语法未破坏。提示词变化使旧真实证据失效；必须安装新候选，从全新聊天完成十二轮和读回后才可锁定。当前仍未完成真实门禁。
