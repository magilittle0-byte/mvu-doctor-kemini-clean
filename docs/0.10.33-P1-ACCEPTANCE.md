# 0.10.33 变量模块阶段记录

P1 的 17 个运行文件保持真实测试字节，指纹为 `062e6f4e2296d860b6e6ce02357efd26ca4b329b890af8ec4a1e3ab088a7015c`，来源提交 `25d15508f255300c9f9c1e89e5d53216d3da914a`。本次锁定仅覆盖变量模块的运行、单次主动修复、持久化与界面机制，按用户允许保留模型例外的标准完成。完整医生尚未完成真实门禁，人物档案和世界引擎的当前候选仍需独立验收。

同一真实聊天完成十二个有效普通回复和一个额外副本压力回复。首次上游 503 保留为无效回复，随后自然 reroll 补成首个有效回复，没有跨聊天补数。压力回复的正文系统名单引入五个独立新人物，MVU 名单和界面一致。人物分散降临，未同时物理在场；协议要求的是“同一回复出现多个新 NPC”，先前把物理同时在场当成必要条件的报告解释已追加更正，原记录不覆盖。

自动 P1 共 13 次、每回合 1 次，无自动重试；主动修复 3 次、每次 1 请求，其中两次修正了目标错误，一次仍遗漏日期信息。后者和后续漏项均保留，不能声称所有变量都正确或修复必然消除幻觉。正文还反复添加玩家未授权的主观内心描述，属于预设约束的已知限制，P1 没有修正文；严格语义、零错误和完整三模块验收均为 false。

完成态只刷新一次。宿主因未自动选择聊天先显示首页；重新打开同一存档后，聊天、MVU、医生记录及卡片 UI 的读回指纹一致。未再刷新或生成，新增模型请求为 0。初次等待失败和恢复过程均保留。

P1 自动处理耗时中位数 28.027 秒，范围 17.184–38.215 秒。十二个普通回合从启动到正文与 P1 首次处理结束，中位数 76.436 秒，范围 52.493–124.612 秒；不含人工审阅等待及后续主动修复。这些不是完整三模块耗时。该批宿主共 57 次请求，其中 P1 自动 13、主动修复 3、正文和其他扩展 41；未换算金额。

三个锁文件记录运行字节、证据摘要与相关合同/回归文件。原始逐回合证据、私有正文和运行锚点只保留在本地；仓库仅包含脱敏阶段摘要。新增锁记录不使旧 P2/P3 锁适用于当前候选。
