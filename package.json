{
  "name": "mvu-doctor-kemini-clean",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:runtime": "node scripts/build-runtime.mjs",
    "verify:runtime": "node scripts/build-runtime.mjs --check",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run verify:runtime && node --check index.js && node --check core.mjs && npm test"
  }
}
